# Testing

Run `python -m unittest -v`. The suite covers progressive tiers, boundaries, flat/category promotions, time eligibility, and invalid input. For a live smoke test run `python app.py` and request `GET /health`.
